/** Re-exported types, so the HUD does not reach across the whole domain barrel. */
export type { Artwork, ExperienceTier, Pathway, Room } from '@/domain';
export type { Capability } from '@/lib/device-tier';
